[build]
  base = "smartel"
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# 이 줄이 중요: 함수 번들에 PDF/JSON 포함
[[functions]]
  directory = "netlify/functions"
  included_files = ["smartel/pdf/**"]


